<UserControl x:Class="CodeScope.Presentation.Features.Archive.ArchiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CodeScope.Presentation.Features.Archive">

    <Grid>

        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>

        <Grid>

            <Grid.ColumnDefinitions>

                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>

            </Grid.ColumnDefinitions>

            <Button Content="Back"
                    Background="#222"
                    Foreground="Gray"
                    Padding="10, 5"
                    Command="{Binding GoBackCommand}"/>

            <TextBlock Text="Archive"
                       Foreground="#C9C"
                       Grid.Column="1"
                       FontSize="30"
                       FontWeight="DemiBold"
                       Margin="10, 0"/>
            
        </Grid>

        <ListBox ItemsSource="{Binding Projects}"
         Grid.Row="1"
         Background="Transparent"
         SelectedItem="{Binding SelectedProject}">
            <ListBox.ItemTemplate>

                <DataTemplate>

                    <Border Margin="5"
                    Padding="10"
                    BorderThickness="1">

                        <StackPanel>

                            <TextBlock FontSize="16" 
                               FontWeight="Bold"
                       Foreground="#c9c"
                       Text="{Binding Name}"/>

                            <TextBlock
                Margin="0,5,0,0"
                Foreground="#999"
                Text="{Binding RootPath}"/>

                        </StackPanel>

                    </Border>

                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>

                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>

            </Grid.ColumnDefinitions>

            <Button Content="Restore"
                    Background="#222"
                    Foreground="#C9C"
                    Padding="10, 5"
                    Grid.Column="1"
                    Command="{Binding RestoreProjectCommand}"/>

            <Button Content="Delete"
                    Background="#222"
                    Foreground="#E79"
                    Grid.Column="2"
                    Padding="10, 5"
                    Command="{Binding DeleteProjectCommand}"/>

        </Grid>
        
    </Grid>

</UserControl>