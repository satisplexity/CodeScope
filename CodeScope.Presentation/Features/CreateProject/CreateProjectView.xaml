<UserControl x:Class="CodeScope.Presentation.Features.CreateProject.CreateProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CodeScope.Presentation.Features.CreateProject">

    <UserControl.Resources>

        <ResourceDictionary Source="/Features/CreateProject/CreateProjectViewModelTemplates.xaml"/>

    </UserControl.Resources>
    
    <Grid>

        <Grid.RowDefinitions>

            <RowDefinition Height="auto"/>
            <RowDefinition/>

        </Grid.RowDefinitions>

        <Grid>

            <Grid.ColumnDefinitions>

                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>

            </Grid.ColumnDefinitions>

            <Button Content="Back"
                    Background="#222"
                    Foreground="Gray"
                    Padding="10, 5"
                    Command="{Binding GoBackCommand}"/>

            <TextBlock Text="Create project"
                       Foreground="#C9C"
                       Grid.Column="1"
                       Margin="10, 0"
                       FontSize="30"
                       FontWeight="DemiBold"/>

        </Grid>

        <ContentControl Grid.Row="1"
                        Content="{Binding CurrentStep}"/>

    </Grid>

</UserControl>