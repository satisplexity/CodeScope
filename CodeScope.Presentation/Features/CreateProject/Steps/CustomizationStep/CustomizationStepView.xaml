<UserControl x:Class="CodeScope.Presentation.Features.CreateProject.Steps.CustomizationStep.CustomizationStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CodeScope.Presentation.Features.CreateProject.Steps.CustomizationStep">

    <Grid>

        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>

        <TextBlock Text="Customization"
                   FontSize="20"
                   FontWeight="DemiBold"
                   Foreground="#99C"/>

        <Button Content="Analyzator settins"
                Background="#222"
                Foreground="#99C"
                Padding="10, 5"
                Grid.Row="2"
                HorizontalAlignment="Left"
                Command="{Binding GoToAnalyzerSettingsStepCommand}"/>

    </Grid>

</UserControl>