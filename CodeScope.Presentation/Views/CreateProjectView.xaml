<UserControl xaml:Class="CodeScope.Presentation.Views.CreateProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:xaml="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CodeScope.Presentation.Views">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button Content="x"
                Width="40"
                Height="40"
                Foreground="White"
                FontSize="20"
                Background="#333"
                HorizontalAlignment="Left"
                Margin="10"/>

        <StackPanel Grid.Row="1">

            <TextBlock Text="Name"
                       Foreground="White"
                       Margin="10"/>

            <TextBox Text="{Binding ProjectName}"
                     Margin="10"/>

            <TextBlock Text="Description"
                       Foreground="White"
                       Margin="10"/>

            <TextBox Text="{Binding ProjectDescription}"
                     Margin="10"/>

            <Button Width="200"
                    Height="80"
                    Content="Create project"
                    Foreground="White"
                    Background="#242424"
                    Margin="10"
                    Command="{Binding CreateProjectCommand}"/>

        </StackPanel>
        
    </Grid>
    
</UserControl>
