<UserControl x:Class="CodeScope.Presentation.Views.ProjectOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodeScope.Presentation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid Background="#19191A">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Background="#212122">

            <RadioButton Content="Snapshots"
                         Foreground="White"
                         Background="#252526"
                         Margin="10"/>

            <RadioButton Content="Properties"
             Foreground="White"
             Background="#252526"
             Margin="10"/>

            <RadioButton Content="Settings"
             Foreground="White"
             Background="#252526"
             Margin="10"/>

        </StackPanel>

        <ContentControl Grid.Column="1"/>

    </Grid>

</UserControl>