<UserControl x:Class="CodeScope.Presentation.Views.SnapshotsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodeScope.Presentation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ListBox ItemsSource="{Binding Snapshots}">
            <ListBox.ItemTemplate>
                <DataTemplate>

                    <Border Padding="10"
                    Margin="0 0 0 10">

                        <StackPanel>

                            <TextBlock Text="{Binding Name}" />

                            <TextBlock Text="{Binding CreatedAt}" />

                            <TextBlock Text="{Binding TotalLines}" />

                        </StackPanel>

                    </Border>

                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="1">
            <Button Style="{StaticResource RoundedButton}"
                    Content="Create snapshot"
                    Command="{Binding CreateSnapshotCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>