<UserControl x:Class="CodeScope.Presentation.Views.Temp.IconsTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CodeScope.Presentation.Views.Temp">
    <Grid>
        <WrapPanel MaxWidth="1000">
            <Path Style="{StaticResource Home}"/>
            <Path Style="{StaticResource PhoneCall}"/>
            <Path Style="{StaticResource Envelope}"/>
            <Path Style="{StaticResource Marker}"/>
            <Path Style="{StaticResource Heart}"/>
            <Path Style="{StaticResource Settings}"/>
        </WrapPanel>
    </Grid>
</UserControl>
